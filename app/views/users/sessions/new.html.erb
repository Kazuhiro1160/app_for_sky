<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="assets/img/favicon.png">
    <title>Slat</title>
    <%= stylesheet_link_tag "css/login", media: 'all' %>
    <%#= stylesheet_link_tag "css/session", media: 'all' %>
  </head>
  <body>
    <div class="center-container">
      <div class="inner-container">
        <span class="glyphicon glyphicon-list-alt"></span>
        <div class="text-center name-display">
          <%= image_tag "slat_logo.svg", alt: "アイコン", size: "150x150" %>
        </div>
        <div class="box">
          <span class="warning-box text-center">
            <span class="text-danger"><%= params[:notice] %></span>
          </span>
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

              <%= f.text_field :login_id, autofocus: true, autocomplete: "off", class: 'form-control', placeholder: "loginID" %>


              <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: "password"%>

            <div class="form-group">
              <%= f.submit "Log in", class: "btn btn-lg btn-primary btn-block" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </body>
</html>